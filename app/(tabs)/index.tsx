import { View, Text } from "react-native";

export default function HomeScreen() {
  return (
    <View className="w-full h-full bg-red-500   flex justify-center items-center">
      <Text className="font-[Bold] text-[30px] font-bold"> Hellp</Text>
    </View>
  );
}
